For executing Mask Detection program
install Python
run command in command prompt: "pip install -r requirements.txt"

open cmd in the folder FaceMask_detection
then run command: "python manage.py runserver"

open localhost 8000 in the browser and click on mask detection to run the program